export type patientType = {
    id?: string
    name: string,
    birthdate: string
    email: string,
    postalCode: string,
    street: string,
    number: string,
    neighborhood: string,
    city: string,
    state: string
}